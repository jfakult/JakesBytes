[Unit]
Description=Yambar status bar
After=graphical-session.target

[Service]
ExecStart=/usr/bin/yambar
ExecStop=/usr/bin/pkill -f yambar
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
